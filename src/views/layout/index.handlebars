<div>
    <h1>¡Hola, {{ user.name }}!, ¡Que gusto verte!</h1>
    <h2>Socket Io</h2>
    {{#if isAdmin }}
    {{#each fakeApi}}
    <div>
        <p>Nombre del producto {{this.name}}</p>
        <p>Precio: {{this.price}}</p>
    </div>
    {{/each}}
    {{else}}
    <h2>Usted no tiene los permisos necesarios para ver los productos</h2>
    {{/if }}

</div>
<div>
    <input type="text" name="chatbokx" id="chatbox">
</div>
<div>
    <ul id="messageLog"></ul>
</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.1/dist/sweetalert2.all.min.js"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="/virtual/js/index.js"></script>
